// Use for creating database in MySQL
CREATE DATABASE registration;


// Use for creating table into database
USE registration;
CREATE TABLE registerInformation(id int PRIMARY KEY AUTO_INCREMENT ,name VARCHAR(30) NOT NULL ,email VARCHAR(45) UNIQUE ,password VARCHAR(100) NOT NULL);

// Use for display the data which are store in the table
USE registration;
SELECT * FROM registerInformation;